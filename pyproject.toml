[project]
name = "course-cli"
version = "0.1.0"
description = "CLI-инструмент для структурирования курса"
dependencies = [
    "click",
    "PyYAML",
    "python-dotenv",
    "pytest",
    "pytest-cov"
]

[project.scripts]
course-cli = "course_cli.cli:main"